{
  "name": "food-delivery",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev:api": "pnpm --filter api run start:dev",
    "dev:mobile": "pnpm --filter mobile run start",
    "dev:mobile:android": "pnpm --filter mobile run android",
    "dev:mobile:web": "pnpm --filter mobile run web",
    "dev": "concurrently \"pnpm dev:api\" \"pnpm dev:mobile\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.1.3",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
